version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16

    commands:
      - npm install

  build:
    commands:
      - npx tailwindcss -i ./public/assets/index.css -o ./public/output.css
      - webpack --config webpack.prod.js

artifacts:
  files:
    - public/**/*